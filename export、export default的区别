1、export 和 export default 区别： 
  
    两者均可导出常量、函数、文件、模块，
    
    你可以在其它文件或模块中通过import+(常量 | 函数 | 文件 | 模块)名的方式，将其导入，以便能够对其进行使用，
    
    但在一个文件或模块中，export、import可以有多个，export default仅有一个。
    
    例如：
     1、使用export 导出：
        export const str = "hello"
        export funciton f(a) {
          return a+1
        }
      
      此时使用import引入时：
        import {str, f} from '...'
        
     2、使用export default 导出：
        export default const str = 'world'
        
        使用 import导入时，
        import str from '...'
      
